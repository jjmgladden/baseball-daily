{
  "name": "baseball-daily-api",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker serving the Baseball Daily AI Q&A proxy + submission form endpoint.",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "tail": "npx wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
